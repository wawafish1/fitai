# 安全说明

## 支持范围

当前只为最新的 `v1.x` 版本提供安全修复。

## 报告问题

请不要在公开 Issue 中粘贴 API Token、腾讯云密钥、验证码、邮箱、数据库、餐食照片或服务器登录信息。优先通过 GitHub 私有安全报告（Private vulnerability reporting）联系仓库所有者；如果仓库尚未开启该功能，请先发送不包含敏感数据的简要说明。

## 密钥管理

- 腾讯云 SES 使用仅具有发信权限的 CAM 子用户，不使用主账号密钥。
- AI Token、`SESSION_SECRET` 和腾讯云密钥只放在服务器 `.env` 或专用密钥管理服务中。
- 不要在任何 `VITE_*` 变量中保存服务端密钥，因为这些变量会进入浏览器资源。
- 怀疑泄露时应立即在服务商后台吊销旧密钥、创建新密钥、更新服务器 `.env` 并重建容器。
- 测试 Token 也应视为真实凭据；测试完成后及时轮换。

## 数据保护

- `/app/data` 包含账号记录、身体数据和餐食照片，应限制为服务账号可访问。
- 备份文件应加密，且不要与服务器访问密钥保存在同一位置。
- 恢复备份前先在隔离环境验证数据库和照片完整性。
- 正式开放公众注册前，应提供账号注销、个人数据导出和照片删除策略。

## 生产检查

- 强制 HTTPS，并确认 HSTS、CSP、`X-Content-Type-Options` 和 `X-Frame-Options` 响应头存在。
- 防火墙只开放 SSH、HTTP 和 HTTPS；数据库与 Node 端口不直接暴露公网。
- SSH 使用密钥登录，关闭密码登录和 root 远程登录，并定期移除临时公钥。
- 定期安装系统和容器安全更新，运行 `npm audit --omit=dev`。
- 监控验证码、AI 调用、磁盘空间、容器重启和 5xx 错误。
