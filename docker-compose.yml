services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - fitai_data:/app/data
    networks:
      - web
      - shared_proxy

networks:
  web:
  shared_proxy:
    external: true
    name: writing-assistant-proxy

volumes:
  fitai_data:
